HIL workflow smoke test
=======================

This is a throwaway marker file used to open a no-op pull request against the
PolyKeyboard branch so the "Build and HIL Test" workflow (.github/workflows/
qmk-test.yml) runs end-to-end:

  1. build      - compile split72 (default + per-side POLYKYBD_HIL left/right) and corne42
  2. hil-test   - flash the left image to the left board and the right image to the
                  right board, then run station.test_runner on the polykybd-ctnd rig

Safe to delete once the run is confirmed green.

Triggered: 2026-05-31
Re-triggered: 2026-06-01 (after polykybd-ctnd#10 picotool flashing fix)
Re-triggered: 2026-06-01 (rig deployed on fixed branch: picotool + UDisks-ignore)
Re-triggered: 2026-06-01 (per-side forced master + hidraw VID 4b50->2021 fix)
