ENCODER_MAP_ENABLE = yes

# Keymap-specific options only. emoji/emoji_layer.c and layers.h are keyboard-level
# (see ../../rules.mk and ../../layers.h) — the shared poly_keymap.c depends on them,
# so a keymap must not be what makes them exist.
